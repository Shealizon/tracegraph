# PaddleOCR

只在用户明确要求 OCR，或 PDF 工具报告 `low_text_pages` 且渲染确认页面为扫描图像时调用 `paddle_ocr`。

- 优先只识别必要页，避免整份长文档的外部上传和等待。
- OCR 结果必须标记为 OCR 派生文本，并保留页码顺序。
- 数学公式、表格、上下标和易混字符必须对照 `pdf_render` 的页面图像复核。
- 工具会把文件发送到管理员配置的 PaddleOCR 外部服务；不得绕过服务器凭据或上传无关文件。
