PORT=8787
HOST=0.0.0.0
TRACEGRAPH_DATA=./server-data
# Python 3.9+ executable used to create one isolated venv per imported tool package.
# TRACEGRAPH_PYTHON=C:\Python312\python.exe
# Required only by the built-in PaddleOCR extension. Keep it on the server.
# PADDLEOCR_TOKEN=
JSON_LIMIT=32mb
# Set to 0 when the host does not provide the `codex` CLI.
CODEX_ENABLED=1
# Optional absolute executable/entrypoint override for non-standard installations.
# CODEX_BIN=/usr/local/bin/codex
# Maximum duration of a single background Codex task in milliseconds.
CODEX_TASK_TIMEOUT_MS=600000
# Hosts outside a Codex-supported region must provide a trusted outbound proxy.
# HTTPS_PROXY=http://127.0.0.1:7890
ALLOW_REGISTRATION=1
# Optional secure bootstrap; set both before the first server start.
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=replace-with-a-long-random-password
